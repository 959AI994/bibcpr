# packages/schemas — Deferred

Cross-runtime schemas (Python + TypeScript) generated from the
Pydantic models in `backend/cpr/schemas.py`. In Phase 1 the schemas
live in Python only.

See [`../../docs/roadmap.md`](../../docs/roadmap.md).
